import { IProductProps } from "../../models/IProductProps";

export const ProductItem = (product: IProductProps) => {
  return (
    <>
      {/* <div className="productContainer">{product.map((p) => {})}</div> */}
    </>
  );
};
