export interface IProduct {
  productTitel: string;
  productDescription: string;
  price: number;
  expireDate: number;
  quantity: number;
}

export interface INavItem {
  label: string;
  href: string;
}
